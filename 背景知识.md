# VLA模型

## 定义与背景

VLA模型是一类将视觉感知、语言理解和机器人动作生成统一在单一神经网络中的端到端基础模型。它接收图像观测和自然语言指令作为输入，直接输出可执行的机器人控制信号，从而把"看懂—听懂—动手"三件事打通。

这一范式由 Google DeepMind 在 RT-2（2023） 中正式提出，其核心论点是：将互联网规模的视觉-语言预训练知识，通过"动作即语言"的方式注入到机器人控制中，从而获得语义理解、推理和泛化能力。此后短短两年内，OpenVLA、Octo、π0、π0.5  、CogACT 等一系列工作涌现，VLA 已成为具身智能领域的主流范式之一。

## 核心思想

VLA 模型的核心思想可以概括为三点：

1. 统一架构：用同一个 Transformer 同时处理视觉、语言和动作三种模态，避免传统模块化流水线（感知→规划→控制）的误差累积。
2. 知识迁移：利用在大规模网络数据上预训练的 VLM（Vision-Language Model）作为骨干，把语义常识、推理能力迁移到物理世界。
3. 跨实体泛化：通过在多机器人、多场景数据上联合训练（cross-embodiment），让单一策略适配不同形态的机器人。

## 代表性工作

### RT-2（2023，Google DeepMind）——开创者

- 架构：基于 PaLM-E（12B）和 PaLI-X（55B）两种 VLM。
- 关键创新——动作即文本：把 7 维动作向量（x, y, z, roll, pitch, yaw, gripper）离散化为文本 token，如 "1 128 91 241 5 101 127 217"，让 LLM 直接"说"出动作。
- Co-fine-tuning：同时训练机器人轨迹数据和原始 VQA 数据，避免灾难性遗忘。
- 能力：相比基线泛化能力提升 \~2×，具备符号理解、链式思维推理（CoT）能力（如"拿石头当锤子"）。
- 意义：首次证明 VLM 的语义知识可迁移到物理控制，定义了 VLA 这一类别。

### Open X-Embodiment 与 RT-X 系列（2023）——跨实体数据集

- 数据集：34 家机构联合贡献，100 万+ 真实机器人轨迹，22 种机器人本体，527 种技能，是迄今最大的开源真实机器人数据集。
- RT-1-X：基于 RT-1 的 Transformer 架构，在小数据场景下比单机器人方法提升 50%。
- RT-2-X（55B）：在涌现技能评估中比 RT-2 提升 3×，展示出更强的空间理解（如对介词的响应）。
- 价值：证明了跨本体数据共享的"正向迁移"可能性，成为后续所有 VLA 的标准预训练语料。

### OpenVLA（2024，Stanford/Berkeley 等）——开源 SOTA

- 规模：7B 参数，基于 Prismatic-7B VLM。
- 视觉编码器：SigLIP + DinoV2 融合，再经 projector 映射到 LLM 空间；骨干为 Llama 27B。
- 训练数据：Open X-Embodiment 中的 97 万条轨迹。
- 性能：超越 RT-1-X、Octo，甚至闭源 55B 的 RT-2-X；是唯一在所有测试任务上成功率 ≥50% 的方法。
- 工程价值：完全开源（PyTorch + HuggingFace），支持 LoRA 微调（仅训练 1.4% 参数即可匹配全量微调），是多机器人适配的事实标准基线。

### Octo（2024，Berkeley）——轻量级通用策略

- 规模：仅 27M（Small）/ 93M（Base），比 VLA 小两个数量级。
- 架构：Transformer 主干 + 扩散解码器，建模多模态动作分布。
- 特点：同时支持语言指令和目标图像条件（RT-X 仅支持语言）；可适配新的传感器输入（如力矩）和新的动作空间（如关节位置控制）。
- 性能：零样本评估中超越 RT-1-X，可与 55B 的 RT-2-X 媲美；微调场景平均比次优基线高 52%。
- 定位：证明小模型 + 扩散头 + 多模态条件也是一种可行路径。

### π0 与 π0.5（2024–2025，Physical Intelligence）——Flow Matching 路线

#### π0

- 基于预训练 3B VLM，新增一个 Action Expert（含 300M 参数）输出连续低层指令，最高 50Hz。
- 核心创新：用 Flow Matching（扩散模型的变体）让 VLM 输出连续动作，绕开"动作离散化"的精度损失。
- 数据：Open X-Embodiment + 私有 π 数据集（8 种机器人，含灵巧任务）。
- 性能：在叠衣服任务上得分 1.0（OpenVLA 和 Octo 均为 0）； grocery bagging 任务 0.786 vs 0。

#### π0.5

- 在 π0 基础上引入异构数据共训练（web + cross-embodiment + 移动操作）。
- 分层规划：同一模型先输出高层文本动作（"告诉自己下一步做什么"），再生成 50 步连续动作块。
- 目标：开放世界泛化，如在陌生家庭中完成清洁等长时程任务。

### CogACT（2024）——组件化设计

- 三模块解耦：视觉模块（DINOv2 + SigLIP）→ 语言模块（Llama 2 7B）→ 独立的动作模块（≤300M）。
- 关键论点：动作不应简单量化为 VLM token，而应用专门模块建模。采用 Diffusion Transformer（DiT）作为动作头，建模连续、多模态、时序相关动作。
- Adaptive Action Ensemble（AAE）：改进 ACT 的动作集成策略，避免不同模态动作的错误聚合。
- 性能：相比同规模 OpenVLA 模拟+35%、真实机器人+55%；相比 55B RT-2-X 模拟 +18%。
- 意义：从架构上反驳了"动作即语言"的简单化处理，重新确立"认知-动作分离"的设计哲学。

## 架构范式对比

| 范式 | 代表 | 动作表示 | 优点 | 缺点 |
| --- | --- | --- | --- | --- |
| 动作离散化为 token | RT-2, OpenVLA | 离散文本 token | 复用 LLM，简洁统一 | 精度损失，难以表达多模态分布 |
| 扩散头解码 | Octo, CogACT | 连续，扩散去噪 | 多模态分布建模好 | 推理慢，需多步采样 |
| Flow Matching | π0, π0.5 | 连续，流匹配 | 高频控制（50Hz），训练稳定 | 工程复杂，数据要求高 |
| 分层 / 双解码 | π0.5, CogACT+CoT | 高层文本 + 低层连续 | 长时程任务，可解释 | 训练目标设计复杂 |

**总体趋势**：从"动作即语言"的简单统一，逐步走向"认知与动作分离、连续动作建模、跨本体大规模共训练、分层长时程规划"的精细化设计。

# 模型蒸馏

## 概念与起源

模型蒸馏是一种模型压缩与知识迁移技术，核心思想是：用一个大而强的教师模型（Teacher）指导训练一个小而精的学生模型（Student），使学生逼近甚至超越教师的性能，同时大幅降低计算与存储开销。

- 提出者：Geoffrey Hinton 等人于 2015 年在论文 Distilling the Knowledge in a Neural Network 中正式提出。
- 灵感来源：工业界 ensemble 模型（如多个 DNN 集成）效果虽好，但部署代价高；蒸馏可将 ensemble 的"知识"压缩到单模型中。

## 基本原理

### 软标签（Soft Labels）与温度（Temperature）

教师模型输出 logits zi，通过带温度参数 T 的 softmax 得到软标签：

$$ q_i = \frac{e^{\frac{z_i}{T}}}{\sum_{j=1}^n e^{\frac{z_j}{T}}} $$

- T→1 退化为标准 softmax，输出趋于 one-hot。
- T→$\infty$ 输出趋于均匀分布，暴露类间相似性（"dark knowledge" 暗知识）。
- 典型取值：T ∈ \[2, 20\]

### 损失函数

学生损失通常为：

$$ L = α · KD + (1 - α) · CE $$

- KD：学生与教师软标签的KL散度，用以衡量二者输出概率分布的相似性
- CE：学生与真实硬标签的交叉熵
- α：权衡系数，常取0.5-0.9

## 主要方法分类

### 按迁移信号类型

| 基于响应（Response-based） | 蒸馏最终 logits / 软标签 | Hinton 2015 |
| --- | --- | --- |
| 基于特征（Feature-based） | 蒸馏中间层特征图/激活 | FitNets \(Romero 2015\) |
| 基于关系（Relation-based） | 蒸馏样本间或层间关系 | RKD \(Park 2019\), CRD |
| 基于注意力（Attention-based） | 蒸馏注意力分布 | TinyBERT, MiniLM |

### 按教师状态

- 离线蒸馏（Offline）：教师预训练固定，最常见。
- 在线蒸馏（Online）：教师与学生同步训练，如 Deep Mutual Learning \(Zhang 2018\)。
- 自蒸馏（Self-distillation）：教师即学生自身深层或多个副本，如 Born-Again Networks、Be Your Own Teacher。

### 按架构对齐

- 同构蒸馏：师生结构相似，仅层数/宽度缩减。
- 异构蒸馏：跨模态、跨架构（如 CNN↔Transformer），需对齐模块。

## 应用场景

- 端侧部署：手机/IoT 上的实时推理。
- 推荐与搜索：双塔/排序模型上线前压缩。
- 视觉任务：检测、分割蒸馏（如 FGD、LD for Detection）。
- 语音识别：wav2vec 等大模型蒸馏。
- 多模态：CLIP、BLIP 类大模型蒸馏为轻量对齐模型。

## 挑战与前沿

### 主要挑战

- 容量鸿沟（Capacity Mismatch）：学生容量不足时，强行模仿教师反受其害（"over-distillation"）。
- 温度与权重调参敏感：无统一最优配置。
- 异构架构对齐难：CNN 与 Transformer 特征分布差异大。
- 任务适配：检测/分割等结构化任务蒸馏难度高于分类。
- 教师误导风险：教师偏见会传递，且学生难以"青出于蓝"。

### 前沿方向

- 数据增强与课程学习：由易到难的数据调度。
- 解耦蒸馏（Decoupled KD, DKD）：将 KL 拆为 target-class 与 non-target-class，分别加权。
- Review-KD：反向蒸馏（学生浅层指导教师深层），缓解对齐错位。
- 生成式蒸馏：用教师合成数据补足分布覆盖。
- 多教师蒸馏：集成多教师互补知识。
- 蒸馏 + 量化/剪枝：组合压缩方案。
- 可解释性蒸馏：迁移注意力/显著性图，提升透明度。

## 评价维度

| 维度 | 指标 |
| --- | --- |
| 性能 | Accuracy / F1 / mAP 等 |
| 压缩比 | 参数量、FLOPs、内存 |
| 推理速度 | 延迟、吞吐 |
| 泛化能力 | OOD、跨域表现 |
| 训练成本 | 教师推理开销、训练时长 |

经验法则：学生参数量减少 10×，性能损失通常在 1%\~5%；

## 总结

模型蒸馏本质是知识表示的迁移，而非简单的标签复制。其优势在于：保留大模型性能的同时显著降低部署成本；其难点在于如何选择迁移信号、对齐架构、平衡温度与权重。随着 LLM 时代的到来，蒸馏已成为开源小模型获取闭源大模型能力的关键路径之一，是当前最活跃的研究方向之一。

# VLA模型蒸馏

## 背景：VLA 模型与压缩需求

### 什么是 VLA 模型

**VLA（Vision-Language-Action）模型**是一类将视觉感知、语言理解与机器人动作输出统一在单一神经网络中的端到端基础模型。它继承自 VLM（Vision-Language Model），通过将"动作"作为新的模态纳入统一训练框架，使大模型从"会看会说"扩展到"会看会说会做"。

典型代表：

- RT-2（Google DeepMind, 2023）：首次将动作离散为文本 token，与互联网图文数据共训练，基于 PaLM-E（12B）/ PaLI-X（55B）。
- OpenVLA（Stanford/Berkeley, 2024）：基于 Prismatic-7B VLM（SigLIP + DinoV2 + Llama 2 7B），在Open X-Embodiment 970K 轨迹上微调，动作 token 化为 256 bin 的离散 token。
- π0（Physical Intelligence, 2024）：在 3B VLM 基础上叠加 Action Expert，用 flow matching（扩散变体）生成连续动作，支持 50Hz 高频控制。
- RDT-1B、CogACT、HPT、Octo 等其他变体。

### 为何需要蒸馏与压缩

VLA 模型面临的核心矛盾：

| 矛盾点 | 说明 |
| --- | --- |
| 推理延迟 vs 控制频率 | 7B 模型自回归生成动作常达数百毫秒，难以满足 10–50Hz 实时控制 |
| 显存占用 vs 端侧部署 | 7B–55B 参数难以部署到机械臂控制器或边缘设备 |
| 数据效率 vs 任务覆盖 | 微调新任务需大量示教数据 |
| 教师能力 vs 学生容量 | VLA 教师拥有"暗知识"（动作多模态、隐含推理），难以完全迁移 |

因此，**蒸馏与压缩**成为 VLA 走向实际部署的关键技术。

## VLA 蒸馏的信号类型

VLA 模型与传统分类模型不同，蒸馏信号需要覆盖多模态、多阶段：

### 响应级蒸馏（Response-based）

- 动作 token 软标签：教师输出的离散动作 token 分布作为软目标（RT-2、OpenVLA 适用）。
- 连续动作分布蒸馏：教师 flow matching / diffusion 的得分函数（score function）或采样轨迹作为目标，学生用 KL/MMD 拟合。

### 特征级蒸馏（Feature-based）

- 对齐 VLM 主干的中间层特征（视觉 token、语言 token）。
- 对齐 Action Expert 的隐状态（如 π0 中 VLM 与 action expert 的 cross-attention 表示）。

### 注意力级蒸馏（Attention-based）

- 蒸馏 VLM 的自注意力分布（类 TinyBERT 做法），帮助学生继承视觉-语言对齐能力。
- 对机器人任务，注意力图往往反映"看哪里抓哪里"的关键空间信息。

### 关系级蒸馏（Relation-based）

- 蒸馏样本间动作相似度关系（RKD-D/A 思想用于动作空间）。
- 蒸馏"任务-动作"对应关系，提升组合泛化。

### 推理链蒸馏（Chain-of-Thought Distillation）

- π0.5 等模型使用"先输出高层文字动作描述，再执行底层动作"的 chain-of-thought 推理。
- 可蒸馏该推理过程，使学生学会分步规划。

## VLA 核心压缩与加速技术

### 动作 Token 化加速

#### OpenVLA 的离散化方案

将 7 维连续动作（末端位姿、夹爪等）每维分桶为 256 bin，转为文本 token 自回归生成。简单但慢。

#### FAST / FAST+ Tokenizer（Pertsch et al., 2024）

- 基于 离散余弦变换（DCT） 的压缩式动作 tokenization。
- 在频率域对动作序列进行压缩，大幅降低 token 数量。
- 在 π0 上集成后：训练时间减少 5×，性能匹配 diffusion VLA。
- FAST+ 在 100 万真实轨迹上训练，成为跨机器人、跨动作空间的通用 tokenizer。
- 本质上是一种"信号级知识压缩"，可视为蒸馏的前置步骤。

### OpenVLA-OFT 优化微调配方（Kim et al., 2025）

四项关键设计联合优化速度与成功率：

| 技术 | 作用 |
| --- | --- |
| 并行解码（Parallel Decoding） | 同时预测多个动作 token，打破自回归瓶颈 |
| Action Chunking | 一次预测一段动作序列（如 8 步），减少推理调用次数 |
| 连续动作表示 | 抛弃离散 bin，直接回归连续动作向量 |
| L1 回归损失 | 替代交叉熵，自然滤除示教噪声，收敛到中位模式 |

效果：相比 OpenVLA 基线，速度提升 26×，延迟降低 3×，在 LIBERO 上达 97.1% 成功率，超越 π0、Diffusion Policy。

虽然 OFT 本质是"优化微调"而非经典师生蒸馏，但其技术被广泛用于 VLA 压缩管线。

### Diffusion Policy 与其加速蒸馏

Diffusion Policy（Cheng Chi et al., 2023）将策略建模为条件去噪扩散过程，可处理多模态动作分布。其加速思路：

- Consistency Distillation：将多步扩散蒸馏为 1–4 步一致性模型。
- Rectified Flow / Flow Matching：π0 用 flow matching 替代离散扩散，路径更直，采样更快。
- Speculative Decoding for Diffusion：用小模型快速起草，大模型校验。

### ACT 与 Action Chunking

ACT（Action Chunking Transformer, 2023）：Tony Zhao 等人提出的 ALOHA 双臂操作系统核心。使用 transformer + VAE 学习动作序列生成模型，预测未来 K 步动作块。其特点：

- 用 CVAE 处理多模态。
- 通过 action chunking + 执行时滑窗，显著降低推理频率需求。
- 本身不是蒸馏，但与并行解码、块预测一起构成 VLA 加速的基础工具集。

## VLA 蒸馏的典型范式

### 跨规模蒸馏：大 VLA → 小 VLA

教师：OpenVLA 7B / RT-2 55B / π0 3B

学生：TinyVLA、SmolVLA、小型 transformer policy

TinyVLA（2024） 关键做法：

- 用预训练的高效多模态模型（如 MobileVLM、1–3B VLM）初始化策略骨干，免去机器人预训练阶段。
- 微调时加入 diffusion policy decoder，替代自回归 token 解码，提高数据效率。
- 在速度与数据效率上"显著优于 OpenVLA"，并具强泛化性。
- 蒸馏信号：继承 VLM 预训练知识（隐式蒸馏）+ diffusion decoder 模仿教师动作分布。

### 跨模态蒸馏：VLM 教师策略 → 纯视觉策略

教师：VLM/VLA（含语言推理）

学生：CNN/小 transformer 策略（仅视觉+本体感觉输入）

蒸馏内容：

- 软动作标签。
- 注意力图（让小模型学会关注关键物体）。
- 中间语义特征（如 CLIP 视觉特征对齐）。

### 跨具身体蒸馏（Cross-Embodiment Distillation）

不同机器人本体（机械臂、人形、四足、灵巧手）的动作空间维度、动力学差异大。HPT（Heterogeneous Pretrained Transformers, 2024）思路：

- 用独立 tokenizer 将不同本体的输入/输出对齐到统一 token 序列。
- 在 52 个数据集上预训练共享 trunk，形成 embodiment-agnostic 表示。
- 微调在新本体上性能提升 20%+。
- 蒸馏视角：教师是多种异构数据上的多面手，学生是任务专用小模型。

### 数据级蒸馏：教师生成示教数据

- 用强 VLA 教师在仿真/真实环境中自主生成轨迹作为"软示教数据"。
- 学生在这些数据上做模仿学习（经典 Behavioral Cloning）。
- π0 的 post-training、RT-2 的 co-fine-tuning 都隐含此思想。
- 类似 LLM 中的"拒绝采样蒸馏"：教师多次采样取最优，学生 SFT。

### 自蒸馏与在线蒸馏

- Born-Again Policy：学生多次迭代以自身为教师，逐步精化策略。
- Deep Mutual Learning：多个 VLA 学生互相蒸馏，在线同步训练，避免单教师偏差。
- 适合数据稀缺的机器人场景。

## VLA 蒸馏的关键技术细节

### 动作空间对齐

| 维度 | 处理方式 |
| --- | --- |
| 离散 vs 连续 | 用 Gumbel-softmax 或 score matching 桥接 |
| 不同维度 | 投影层（MLP/线性）对齐 |
| 不同频率 | 重采样 / chunking 对齐 |
| 不同坐标系 | 标准化（如转 base frame）后蒸馏 |

### 损失函数组合

- 动作 token / 连续动作的 KL 散度或 MSE。
- 中间特征 MSE/Smooth-L1（含投影层）。
- 注意力分布 KL。
- 教师推理链的 SFT 损失。
- 硬标签任务损失（环境交互得到的真实动作）。

### 温度参数的特别作用

VLA 动作分布常呈多模态（如可以左抓或右抓），温度 T 调控：

  • 高 T ：暴露多模态结构（暗知识）。

  • 低 T ：聚焦主模态。

### 异步与流式蒸馏

机器人需要闭环控制，不能等教师完整推理：

- 教师预计算缓存：离线批量生成软标签。
- 流式蒸馏：学生先出动作，教师异步校验，下一帧修正。
- 类似 LLM 推测解码思想。

## 典型压缩组合管线

现代 VLA 部署常将多种技术串联：

    大 VLA 教师 \(OpenVLA 7B / π0\)

        │

        ▼ \[FAST+ tokenization\]          \# 动作信号压缩

        │

        ▼ \[Action chunking + parallel\]  \# 减少推理步数 \(OFT\)

        │

        ▼ \[Feature + Attention KD\]      \# 蒸馏中间知识

        │

        ▼ \[Diffusion/Flow → 1-step\]     \# 一致性蒸馏

        │

        ▼ \[LoRA / 量化 INT8/INT4\]       \# 参数级压缩

        │

        ▼ \[TinyVLA / SmolVLA 学生\]      \# 1B 左右实时部署

## 与经典 NLP/CV 蒸馏的差异

| 方面 | 经典蒸馏 | VLA 蒸馏 |
| --- | --- | --- |
| 输出空间 | 类别 logits | 离散 token / 连续动作 / 扩散分布 |
| 多模态 | 一般单模态 | 视觉+语言+本体感觉+动作 |
| 时序依赖 | 多为单步 | 必须考虑轨迹、chunking |
| 闭环反馈 | 无 | 学生动作影响下一观测（部分可观测 MDP） |
| 评测 | Accuracy/F1 | 成功率、完成时间、轨迹质量 |
| 数据收集成本 | 低 | 高（需真机/仿真示教） |
| 教师误导后果 | 分类错误 | 可能导致物理事故 |

最后一点尤为重要：VLA 蒸馏需考虑安全性，教师的偏差（如危险抓取姿态）会原样传递，需引入**约束蒸馏**（constrained KD）。
